import React from "react";
import { motion } from "framer-motion";
import { Sparkles, Target, Zap } from "lucide-react";

const toneMessages = {
  sweet: ["You've got this! 💕", "One step at a time, friend"],
  sarcastic: ["Another productive day, right? 😏", "Let's pretend these tasks matter"],
  drill: ["Get to work! No excuses! 🪖", "Time is ticking, soldier!"],
  unhinged: ["DO THE THING! 🤪", "Zombies are coming for your GPA!"]
};

export default function WelcomeBanner({ user, taskCount }) {
  const messages = toneMessages[user.motivation_tone] || toneMessages.sweet;
  const message = messages[Math.floor(Math.random() * messages.length)];

  return (
    <motion.div
      initial={{ opacity: 0, y: -20 }}
      animate={{ opacity: 1, y: 0 }}
      className="glass-card rounded-2xl p-6 mb-6 border-none shadow-lg"
    >
      <div className="flex items-start justify-between">
        <div className="flex-1">
          <div className="flex items-center gap-2 mb-2">
            <span className="text-3xl">👋</span>
            <h2 className="text-xl font-bold text-gray-900">
              Welcome back, {user.full_name || user.email.split('@')[0]}!
            </h2>
          </div>
          <p className="text-gray-600 text-lg mb-4">{message}</p>
          
          <div className="flex flex-wrap gap-4">
            <div className="flex items-center gap-2 bg-teal-50 px-4 py-2 rounded-lg">
              <Target className="w-5 h-5 text-teal-600" />
              <span className="text-sm font-medium text-teal-900">
                {taskCount} active tasks
              </span>
            </div>
            <div className="flex items-center gap-2 bg-purple-50 px-4 py-2 rounded-lg">
              <Zap className="w-5 h-5 text-purple-600" />
              <span className="text-sm font-medium text-purple-900">
                {user.xp || 0} XP
              </span>
            </div>
            <div className="flex items-center gap-2 bg-pink-50 px-4 py-2 rounded-lg">
              <Sparkles className="w-5 h-5 text-pink-600" />
              <span className="text-sm font-medium text-pink-900">
                {user.badges?.length || 0} badges
              </span>
            </div>
          </div>
        </div>
        
        <div className="hidden md:block">
          <div className="w-20 h-20 bg-gradient-to-br from-teal-400 to-purple-600 rounded-full flex items-center justify-center">
            <span className="text-5xl">🧟</span>
          </div>
        </div>
      </div>
    </motion.div>
  );
}
